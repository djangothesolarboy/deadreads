extends layout.pug

append head
    link(rel="stylesheet" href="/stylesheets/reviews.css")

block content
    ul.reviews-container
        .review-container
            each review in jsonReview
                img(src=`/${books[0][0].coverArt}` class='review-cover')
                a(href="/users" class="review-user") User
                li.review= review.review
