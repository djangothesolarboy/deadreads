extends layout.pug

append head
    link(rel="stylesheet" href="/stylesheets/reviews.css")

block content
    ul.reviews-container
        .review-container
            each review in reviews
                img(src=`/${review.Book.coverArt}` class='review-cover')
                a(href="/users" class="review-user") User
                li.review= review.review
